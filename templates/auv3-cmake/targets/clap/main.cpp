#include <stdio.h>

// This is the entry point for the CLAP plugin bundle.
// The actual logic is in the 'plugin' and 'wrapper-clap' static libraries.

void clap_main() {
    printf("CLAP plugin initialized\n");
}
